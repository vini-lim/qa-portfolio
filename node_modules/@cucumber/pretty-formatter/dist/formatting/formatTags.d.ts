import { PickleTag, Tag } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatTags(tags: ReadonlyArray<Tag | PickleTag>, theme: Theme, stream: NodeJS.WritableStream): string;
