import { Pickle, Scenario } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatPickleTitle(pickle: Pickle, scenario: Scenario, theme: Theme, stream: NodeJS.WritableStream): string;
