import { TestStepResultStatus } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatError(message: string, status: TestStepResultStatus, theme: Theme, stream: NodeJS.WritableStream): string;
