import { Hook, TestStepResultStatus } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatHookTitle(hook: Hook | undefined, status: TestStepResultStatus, theme: Theme, stream: NodeJS.WritableStream): string;
