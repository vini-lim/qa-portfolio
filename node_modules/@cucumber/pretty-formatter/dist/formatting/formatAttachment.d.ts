import { Attachment } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatAttachment(attachment: Attachment, theme: Theme, stream: NodeJS.WritableStream): string;
