import { SourceReference } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatSourceReference(sourceReference: SourceReference, theme: Theme, stream: NodeJS.WritableStream): string;
