import { TestStepResultStatus } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatForStatus(status: TestStepResultStatus, text: string, theme: Theme, stream: NodeJS.WritableStream): string;
