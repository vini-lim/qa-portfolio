import { StepDefinition } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatAmbiguousStep(stepDefinitions: readonly StepDefinition[], theme: Theme, stream: NodeJS.WritableStream): string;
