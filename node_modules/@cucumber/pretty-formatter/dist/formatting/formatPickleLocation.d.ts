import { Location, Pickle } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatPickleLocation(pickle: Pickle, location: Location | undefined, theme: Theme, stream: NodeJS.WritableStream): string;
