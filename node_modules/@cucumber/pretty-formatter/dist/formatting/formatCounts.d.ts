import { TestStepResultStatus } from '@cucumber/messages';
import { Theme } from '../types';
export declare function formatCounts(suffix: string, counts: Partial<Record<TestStepResultStatus, number>>, theme: Theme, stream: NodeJS.WritableStream): string;
